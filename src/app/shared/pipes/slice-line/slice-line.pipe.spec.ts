import { SliceLinePipe } from './slice-line.pipe';

describe('SliceLinePipe', () => {
  it('create an instance', () => {
    const pipe = new SliceLinePipe();
    expect(pipe).toBeTruthy();
  });
});
